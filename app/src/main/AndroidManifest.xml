<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prvavjezba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CreateNewRecordActivity"></activity>
        <activity android:name=".adapters.StudentAdapter" />
        <activity android:name=".MyDataStorage" />
        <activity android:name=".Student" />
        <activity android:name=".activities.PocetniActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SummaryActivity"
            android:label="SummaryActivity" />
        <activity
            android:name=".activities.StudentInfoActivity"
            android:label="StudentInfoActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="PersonalInfoActivity" />
    </application>

</manifest>